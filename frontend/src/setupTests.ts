// src/setupTests.ts

// Adds custom jest matchers from @testing-library/jest-dom for DOM assertions like toBeInTheDocument()
import '@testing-library/jest-dom';

// You can add other global test setup here if needed in the future